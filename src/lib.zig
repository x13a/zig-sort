pub const heap = @import("sort_heap.zig").sort;
pub const insertion = @import("sort_insertion.zig").sort;
pub const merge = @import("sort_merge.zig").sort;
pub const quick = @import("sort_quick.zig").sort;
pub const selection = @import("sort_selection.zig").sort;
pub const shell = @import("sort_shell.zig").sort;
